type: remote
hosts:
  - localhost:10022
path: /mount/test.csv
auth:
  user: ubuntu
  type: password
  password: passw0rd
  skip_host_key_verification: true
parser:
  type: csv
  columns:
  - {name: id, type: long}
  - {name: username, type: string}
